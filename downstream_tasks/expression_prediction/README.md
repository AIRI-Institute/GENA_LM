# Fine-Tuning the Expression Model

## Model Execution
To fine-tune the model, run the following command:

```bash
bash finetune_expression_rm.sh
```

## Configuration
Model and dataset parameters are specified in the `run_config.yaml` file:

```
expression_prediction/configs/run_config.yaml
```

## Dataset Configuration
The dataset configuration in the YAML file includes the following fields:

```yaml
train_dataset_forward:
  _target_: <Dataset class>
  intervals_path: Path to sample intervals (genes), stored in `expression_prediction/intervals`
  targets_path: CSV file specifying selected rows from `file_mappings`. Required fields: `id`, `csv`, `forward_bw`, `reverse_bw`
  gen_max_seq_len: Sequence length in tokens, calculated as `512 - (3 + num_mem_tokens) * max_n_segments`
  num_before: Number of tokens before TSS
  genome: Path to the genome file
  gen_tokenizer: Path to the tokenizer
  text_data_path: Path to a `.pkl` file containing dataset descriptions, generated by `expression_prediction/description.py`
  bw: Boolean (`True`/`False`) indicating whether coverage data is used
  tpm: Boolean (`True`/`False`) indicating whether TPM values are used
  transform_targets_bw: Transformation function for coverage data
  transform_targets_tpm: Transformation function for TPM values
```

## File Mappings

Within a single dataset, it is not recommended to include keys for samples of different types. Samples are considered different types based on the presence of coverage data, TPM values, or both.
For example:

- Dataset 1 should include all keys with coverage data.
- Dataset 2 should include all keys with TPM values.
- Dataset 3 should include keys with both TPM and coverage (bw) data.

Each `file_mappings` entry must contain the same number of keys across datasets.

## To-Do
- Implement weight adjustments for TPM and coverage loss.
- Enable support for specifying different numbers of keys in `file_mappings`.
- Add separate transformation functions for TPM and coverage data (BW).


